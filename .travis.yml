sudo: false
dist: xenial

services:
  - postgresql

language: python

python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
  - 3.7

install:
  - pip install tox-travis

before_script:
  - psql -c 'create database timezone_field_tests;' -U postgres

script:
  - tox

after_success:
  - coveralls
